# Copy this file to .env and adjust values as needed.

DATABASE_URL="mysql://matriz_user:matriz_pass@localhost:3306/matriz_dev"

MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=matriz_dev
MYSQL_USER=matriz_user
MYSQL_PASSWORD=matriz_pass
MYSQL_PORT=3306

# Protegem apenas a importação de dados (/upload) — o restante do sistema é público.
ADMIN_PASSWORD=changeme
ADMIN_SESSION_SECRET=changeme-generate-a-random-string
